run = "bash start.sh"
entrypoint = "start.sh"

[nix]
channel = "stable-24_05"

[deployment]
run = ["sh", "-c", "cd backend && npm install && npx prisma generate && npx prisma migrate deploy && npm start & cd ../frontend && npm install && npm run build && npm start"]
deploymentTarget = "cloudrun"
